# How to install meshMaker.py module using pyproject.toml and pip install in a venv
To pip install in an virtual environment the module meshMaker.py, type
```bash
pip install ../FeeLLGood/python-modules/
```
with the appropriate path
